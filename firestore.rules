// ======================================================================
// !! 重要：手動更新 Firestore 安全規則 !!
//
// 由於權限錯誤持續存在，請依照以下步驟手動更新您的安全規則，
// 這將能最確實地解決「Missing or insufficient permissions」錯誤。
//
// 1. 複製此檔案中的所有內容 (從 "rules_version = '2';" 開始)。
//
// 2. 前往您的 Firebase 主控台：
//    https://console.firebase.google.com/
//
// 3. 在主控台中，選擇您的專案 (finlit-classroom)。
//
// 4. 在左側的選單中，點擊「建構」(Build)，然後選擇「Firestore Database」。
//
// 5. 點擊頁面上方的「規則」(Rules) 分頁。
//
// 6. 刪除編輯器中現有的所有文字。
//
// 7. 將您在步驟 1 中複製的內容，完整地貼到編輯器中。
//
// 8. 點擊「發布」(Publish) 按鈕。
//
// 完成後，請重新整理您的應用程式頁面。
// ======================================================================

rules_version = '2';

// 警告：這些規則允許任何人讀取和寫入您的資料庫。
// 這在開發初期很方便，但在應用程式正式上線前，
// 您必須設定更安全、更嚴謹的規則來保護您的資料。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
